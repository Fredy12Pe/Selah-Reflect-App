/**
 * Empty module shim
 * 
 * This is used to replace Node.js-only modules (like grpc-js) that aren't compatible with the browser
 * but might be imported by some Firebase code paths.
 */

module.exports = {}; 